[pytest]
# Add the backend root to sys.path so `from app.main import app` resolves
# whether tests are invoked as `pytest tests/` or `python -m pytest tests/`.
pythonpath = .
testpaths = tests
addopts = -q
